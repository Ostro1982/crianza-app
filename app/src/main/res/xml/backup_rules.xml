<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Excluir la base de datos completa (contiene token Telegram, contraseña email) -->
    <exclude domain="database" path="crianza_db" />

    <!-- Excluir SharedPreferences con el familyId y sesión Google -->
    <exclude domain="sharedpref" path="crianza_family" />
    <exclude domain="sharedpref" path="crianza_prefs" />
</full-backup-content>
