<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Ruta para APKs descargados via DownloadManager para el auto-update -->
    <external-files-path name="downloads" path="." />
    <!-- QR compartido como imagen desde cache interna -->
    <cache-path name="shared" path="." />
</paths>
